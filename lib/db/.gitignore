# Generated by `drizzle-kit generate` at build time and consumed by
# apply-schema.mjs at runtime. This project uses the push-based workflow
# (see drizzle.config.ts), so the generated DDL is a build artifact and is
# NOT committed — the Render build regenerates it on every deploy.
drizzle/
