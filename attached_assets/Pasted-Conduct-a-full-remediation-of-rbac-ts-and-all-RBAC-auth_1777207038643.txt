Conduct a full remediation of `rbac.ts` and all RBAC/auth integrations across the MTOS CRM backend.

OBJECTIVE:
Replace any unsafe/inconsistent authorization logic with production-grade RBAC enforcement.

SCOPE OF WORK:

1. REMOVE ALL AUTH BYPASSES

* Eliminate any god-mode/admin bypass logic such as `user.id !== 0`, implicit allow branches, fallback admin assumptions, or hidden dev shortcuts.
* No authorization bypasses may exist in production code.

2. STRICT ENVIRONMENT GATING

* Any development-only override must require:
  `NODE_ENV === "development"`
* Dev overrides must be explicit, isolated, and logged.
* Production must hard-fail if dev bypass is attempted.

3. CENTRALIZE RBAC ENGINE

* Refactor `rbac.ts` into single source of truth for:

  * Roles
  * Permissions
  * Role→Permission mappings
  * Authorization helpers
* Remove duplicated permission logic elsewhere in codebase.

4. ENFORCE ROUTE-LEVEL AUTHORIZATION

* Audit every API route and backend action.
* Ensure each protected route explicitly requires:

  * Authentication
  * Permission/role validation
* Deny-by-default if no permission mapping exists.

5. NORMALIZE ERROR HANDLING

* Unauthorized → 401
* Forbidden → 403
* Return standardized error envelope for all auth failures.

6. ADD AUDIT LOGGING

* Log all denied authorization attempts.
* Log actor, route/action, timestamp, reason.
* Log all privileged/admin actions.

7. VALIDATE PERMISSION COVERAGE

* Confirm sensitive MTOS operations are protected:

  * Lead management
  * Case management
  * Review queue
  * CSV/exports
  * User management
  * Vendor management
  * Signed retainers/docs
  * Background checks
  * Admin/system settings

8. TESTING / VERIFICATION

* Add/Update tests for:

  * Allowed access per role
  * Denied access per role
  * Missing auth
  * Invalid role
  * Production bypass prevention
* Run end-to-end verification to ensure frontend/backend behavior remains functional.

DELIVERABLES:

1. Patched RBAC/auth code
2. Summary of all vulnerabilities fixed
3. List of routes/actions protected
4. Any residual risks / architectural concerns found

STANDARD:
Production-grade only.
No temporary patches.
No TODOs.
No partial fixes.
Complete end-to-end RBAC hardening.
