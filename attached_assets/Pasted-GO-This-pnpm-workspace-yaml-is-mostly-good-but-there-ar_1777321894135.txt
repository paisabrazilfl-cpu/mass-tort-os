GO ✅

This `pnpm-workspace.yaml` is mostly good, but there are **3 fixes** I’d make.

## Fixed Version

```yaml
packages:
  - artifacts/*
  - lib/*
  - lib/integrations/*
  - scripts

autoInstallPeers: false
strictPeerDependencies: false

catalog:
  '@replit/vite-plugin-cartographer': ^0.5.1
  '@replit/vite-plugin-dev-banner': ^0.1.2
  '@replit/vite-plugin-runtime-error-modal': ^0.0.6
  '@tailwindcss/vite': ^4.2.1
  '@tanstack/react-query': ^5.90.21
  '@types/node': ^25.3.5
  '@types/pdfkit': ^0.13.9
  '@types/react': ^19.2.14
  '@types/react-dom': ^19.2.3
  '@vitejs/plugin-react': ^5.1.4
  class-variance-authority: ^0.7.1
  clsx: ^2.1.1
  drizzle-orm: ^0.45.1
  framer-motion: ^12.35.1
  lucide-react: ^0.545.0
  react: 19.1.0
  react-dom: 19.1.0
  tailwind-merge: ^3.5.0
  tailwindcss: ^4.2.1
  tsx: 4.21.0
  vite: ^7.3.1
  zod: ^3.25.76

minimumReleaseAge: 1440

minimumReleaseAgeExclude:
  - '@replit/*'
  - stripe-replit-sync

onlyBuiltDependencies:
  - '@img/sharp-libvips-linux-x64'
  - '@swc/core'
  - esbuild
  - msw
  - sharp
  - unrs-resolver

overrides:
  '@esbuild-kit/esm-loader': npm:tsx@^4.21.0
  esbuild: 0.27.3
```

## Major Fixes

### 1. Add `strictPeerDependencies`

Your lockfile had this behavior already:

```yaml
strict-peer-dependencies=false
```

So declare it explicitly:

```yaml
strictPeerDependencies: false
```

### 2. Add missing built deps

Your root `package.json` allowed:

```txt
sharp
esbuild
@img/sharp-libvips-linux-x64
```

But this workspace file only had:

```txt
@swc/core
esbuild
msw
unrs-resolver
```

Merged version should include all needed native build deps.

### 3. Remove excessive platform override spam

Your override block is enormous and brittle. Keep only:

```yaml
overrides:
  '@esbuild-kit/esm-loader': npm:tsx@^4.21.0
  esbuild: 0.27.3
```

Let pnpm resolve platform packages naturally unless Replit deployment is breaking from optional packages.

## Run

```bash
pnpm install
pnpm run typecheck
pnpm run ci
```

```txt
PNPM_WORKSPACE_FIX
├── explicit peer policy
├── catalog aligned to lockfile versions
├── pdfkit types catalog added
├── native build deps merged
├── override block simplified
└── regenerate lockfile with pnpm install
```
